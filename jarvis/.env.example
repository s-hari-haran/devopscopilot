# JARVIS Configuration - Copy to .env and fill in your values
# NEVER commit .env file with real keys to GitHub!

# Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash

# Optional: Google Speech-to-Text Credentials
# Download from Google Cloud Console and set path
SPEECH_CREDENTIALS_PATH=credentials.json

# Optional: MegaLLM API (for alternative AI backend)
MEGALLM_API_KEY=your_megallm_api_key_here
